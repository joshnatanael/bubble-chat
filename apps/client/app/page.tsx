import { Button } from "@repo/ui/button";

const Page = (): JSX.Element => {
  return (
    <main>
      <Button color="primary" variant="contained">
        test
      </Button>
    </main>
  );
};

export default Page;
